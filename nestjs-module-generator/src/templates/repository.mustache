import { EntityManager } from '@mikro-orm/core';
import { Injectable } from '@nestjs/common';
import { EntityRepository } from '@/infra/persistence/mikro-orm/repositories/_base-mikro-orm-db.repository';
import { {{singularPascal}}DbPort } from '@/core/{{featureKebab}}/{{featureKebab}}.module';
import { {{singularPascal}} } from '@/core/{{featureKebab}}/domain/entities/{{singularKebab}}.entity';

@Injectable()
export class {{singularPascal}}MikroOrmDbRepository extends EntityRepository({{singularPascal}}) implements {{singularPascal}}DbPort {
  constructor(private readonly em: EntityManager) {}
} 